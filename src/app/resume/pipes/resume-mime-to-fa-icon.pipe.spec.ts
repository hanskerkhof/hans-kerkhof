import { ResumeMimeToFaIconPipe } from './resume-mime-to-fa-icon.pipe';

describe('ResumeMimeToFaIconPipe', () => {
  it('create an instance', () => {
    const pipe = new ResumeMimeToFaIconPipe();
    expect(pipe).toBeTruthy();
  });
});
