import { ResumeReplacePipe } from './resume-replace.pipe';

describe('ResumeReplacePipe', () => {
  it('create an instance', () => {
    const pipe = new ResumeReplacePipe();
    expect(pipe).toBeTruthy();
  });
});
